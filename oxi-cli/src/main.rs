//! oxi — grok-build TUI with oxi-ai backend.

fn main() -> anyhow::Result<()> {
    println!("oxi: grok-build TUI ready.");
    println!("Full integration pending — grok pager compiles in workspace.");
    Ok(())
}
