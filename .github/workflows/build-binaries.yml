name: Build Binaries

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 1'

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── macOS Apple Silicon (only supported target) ──
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact_name: oxicode
            archive_ext: tar.gz

    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: |
          cargo build \
            --release \
            --target ${{ matrix.target }} \
            -p oxicode-cli

      - name: Strip binary
        run: strip target/${{ matrix.target }}/release/${{ matrix.artifact_name }} || true

      - name: Package
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../oxicode-${{ matrix.target }}.tar.gz ${{ matrix.artifact_name }}

      - uses: actions/upload-artifact@v7
        with:
          name: oxicode-${{ matrix.target }}
          path: oxicode-${{ matrix.target }}.${{ matrix.archive_ext }}
