// Stubbed — oxi doesn't use grok's placeholder images system.
use ratatui::buffer::Buffer;
use ratatui::layout::Rect;
#[derive(Clone)]
pub struct PastedImage;
#[derive(Clone)]
pub struct VideoViewerState;
pub fn placeholder_images_module_public_api() {}
