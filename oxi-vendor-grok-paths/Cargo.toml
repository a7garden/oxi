[package]
license = "Apache-2.0"
edition = "2024"
name = "oxi-vendor-grok-paths"
version = "0.1.0"
description = "Type-safe path wrappers for absolute and relative UTF-8 paths"

[dependencies]
camino = "1.1.10"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"

[lints]
