[build]
# sccache for distributed compilation cache
# Enable via: RUSTC_WRAPPER=sccache cargo build

[term]
verbose = false

# Faster incremental builds in dev: build dependencies with mild optimization
# to avoid recompiling them on every change.
[profile.dev.package."*"]
opt-level = 1