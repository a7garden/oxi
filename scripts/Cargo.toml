[package]
name = "generate-models"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "generate-models"
path = "generate-models.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
